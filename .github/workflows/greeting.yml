jobs:
  test-action:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - uses: zukash/greetinig
        with:
          name: "GitHub Actions"
      - run: echo ${{ steps.test-action.outputs.greeting }}
